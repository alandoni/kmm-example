CREATE TABLE Rocket (
    id TEXT NOT NULL PRIMARY KEY,
    name TEXT NOT NULL,
    type TEXT NOT NULL
);

insertRocket:
INSERT INTO Rocket(
    id,
    name,
    type
) VALUES(?, ?, ?);

selectRocketById:
SELECT * FROM Rocket WHERE id = ?;

removeAllRockets:
DELETE FROM Rocket;